meta {
  name: update status
  type: http
  seq: 2
}

put {
  url: {{base}}/contract-review-actions/96/update-status
  body: json
  auth: inherit
}

params:query {
  ~status: cccc
}

headers {
  branch: 11
  Accept: application/json
}

body:json {
  {
      "status":"completed"
  }
  
}

settings {
  encodeUrl: true
}
